# 测试用例（LX Platform 全系统）

> 覆盖范围：认证 / 用户 / 邀请 / 商品 / 订单 / 撮合 / 账户 / 财务 / 工单 / 后台管理 / 文件上传 / 信用等级。
> 文档版本：2026-08-19　对应后端 `backend` + 用户端 `web` + 管理端 `admin`。

## 0. 测试准备

### 0.1 环境

| 项 | 地址 |
| --- | --- |
| 后端 API | `http://localhost:8000`（健康检查 `GET /health` → `200`） |
| 用户端 | `http://localhost:5174` |
| 管理端 | `http://localhost:5173/admin/` |
| 开发库 | `backend\lx_platform.db`（SQLite），Alembic 已升级到 head |

### 0.2 测试账号

| 账号 | 密码 | 角色 | 说明 |
| --- | --- | --- | --- |
| `superadmin` | `Admin@123456` | 超管 | 有全部权限 |
| `parent_a` | `Passw0rd1` | 用户 | users.id=1，已有完成订单 |
| `child_a` | `Passw0rd1` | 用户 | users.id=2，parent_a 直推下级 |

### 0.3 请求约定

- 统一响应包：`{"code":0,"message":"...","data":...,"requestId":"..."}`，`code=0` 表示成功，非 0 为业务错误码。
- 需鉴权接口携带请求头：`Authorization: Bearer <access_token>`。
- 登录获取 token（Windows PowerShell 下用文件方式避免引号问题）：

```powershell
$tmp = "C:\Users\l1910\AppData\Local\Temp\opencode"
$f = "$tmp\login.json"
(@{username="parent_a"; password="Passw0rd1"} | ConvertTo-Json -Compress) | Set-Content -LiteralPath $f -Encoding ASCII -NoNewline
$resp = curl.exe -s -X POST "http://localhost:8000/api/v1/auth/login" -H "Content-Type: application/json" --data-binary "@$f"
$tok = [regex]::Match($resp, '"access_token":"([^"]+)"').Groups[1].Value
# 后续请求： -H "Authorization: Bearer $tok"
```

### 0.4 系统邀请码

系统种子邀请码 `creator_id` 为空（系统发放），查库获取：`backend\lx_platform.db` 表 `invite_codes` 中 `creator_id IS NULL AND status='ACTIVE'`。

---

## 1. 认证模块 `/api/v1/auth`

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| A-01 | 注册成功 | POST `/register` | `{"username":"新号","invite_code":"系统码","password":"Passw0rd","nickname":"昵称"}` | 200，`code=0`，返回 `access_token`/`refresh_token` |
| A-02 | 重复用户名 | POST `/register` | 用已注册用户名 | 400 `USER_EXISTS` |
| A-03 | 邀请码错误 | POST `/register` | `invite_code` 传不存在码 | 400 `INVITE_INVALID` |
| A-04 | 注册限流 | POST `/register` 连续 6 次（同 IP） | 合法参数 | 第 6 次起 429 `RATE_LIMITED` |
| A-05 | 登录成功 | POST `/login` | `{"username":"parent_a","password":"Passw0rd1"}` | 200，返回 token 与用户信息 |
| A-06 | 密码错误 | POST `/login` | 密码错误 | 401 `AUTH_INVALID` |
| A-07 | 登录限流 | POST `/login` 连续 11 次 | 合法参数 | 第 11 次起 429 `RATE_LIMITED` |
| A-08 | 刷新令牌 | POST `/refresh` | `{"refresh_token":"A-05 的 refresh_token"}` | 200，返回新 `access_token` |
| A-09 | 用 access 当 refresh | POST `/refresh` | 传 access_token | 401 `TOKEN_INVALID` |
| A-10 | 当前用户信息 | GET `/me` | - | 200，含 `username/status/credit_level_name/completed_order_count` |
| A-11 | 上级切换登录 | POST `/switch-user` | `{"user_id":子账号id}` | 200，返回子账号 token，`GET /me` 为该子账号 |
| A-12 | 非直推不可切换 | POST `/switch-user` | 子账号 token 切自己 | 401 `SWITCH_FORBIDDEN` |
| A-13 | 未登录访问 | GET `/me` | 无 token | 401 |

---

## 2. 用户模块 `/api/v1/users` + 邀请 `/api/v1/invites`

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| U-01 | 修改资料 | PUT `/me/profile` | `{"nickname":"新昵称"}` | 200，昵称更新 |
| U-02 | 提交实名 | POST `/me/realname` | `{"name":"张三","id_number":"110101199001011234","document_front":"","document_back":""}` | 200，`status=PENDING` |
| U-03 | 查询实名状态 | GET `/me/realname` | - | 200，含 `status/name/id_number`（姓名脱敏） |
| U-04 | 修改密码 | POST `/me/password` | `{"old_password":"旧","new_password":"新"}` | 200；旧密码错误则 400 |
| U-05 | 团队汇总 | GET `/team/summary` | - | 200，含直推人数/团队人数 |
| U-06 | 可切换账号列表 | GET `/team/switchable` | - | 200，`items` 为其直推下级 |
| U-07 | 生成邀请码 | POST `/invites/codes` | `{"count":2}` | 200，返回 2 个 `ACTIVE` 码 |
| U-08 | 我的邀请码列表 | GET `/invites/codes` | - | 200，注册即自动生成 1 个专属码 |
| U-09 | 停用邀请码 | POST `/invites/codes/{id}/disable` | - | 200，该码状态 `DISABLED`，不可再用 |

---

## 3. 商品模块 `/api/v1/products`

| 编号 | 用例 | 方法/路径 | 预期结果 |
| --- | --- | --- | --- |
| P-01 | 商品分类 | GET `/categories` | 200，分类列表 |
| P-02 | 商品列表 | GET ``（`/api/v1/products`） | 200，`items` 含 3 个服务商品（标准/企业/定制） |
| P-03 | 商品详情 | GET `/{id}` | 200，含 `price/service_days` |

---

## 4. 订单模块 `/api/v1/orders`

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| O-01 | 创建买单(商品) | POST `` | `{"order_type":"BUY","product_id":1,"quantity":1}` | 200，状态 `WAITING_PAYMENT`，`service_fee=总价5%`，`payable_amount=总价+费` |
| O-02 | 创建买单(自定义金额) | POST `` | `{"order_type":"BUY","amount":1000}` | 200，`total_amount=1000.00`，`service_fee=50.00`，`payable_amount=1050.00` |
| O-03 | 创建卖单 | POST `` | `{"order_type":"SELL","amount":3000}` | 200，状态 `WAITING_MATCH`，无服务费 |
| O-04 | 创建服务单 | POST `` | `{"order_type":"SERVICE","product_id":3,"quantity":1}` | 200，状态 `WAITING_PAYMENT`（走支付流程，非旧版直接撮合） |
| O-05 | 非法类型 | POST `` | `{"order_type":"XXX"}` | 422 校验失败 |
| O-06 | 支付成功 | POST `/{id}/pay` | - | 200，余额冻结，进入撮合；有卖方时直接撮合成功 |
| O-07 | 余额不足支付 | POST `/{id}/pay` | 余额不足的订单 | 400 `INSUFFICIENT_BALANCE` |
| O-08 | 重复支付 | POST `/{id}/pay` 已支付订单 | - | 400 `ORDER_INVALID_STATUS` |
| O-09 | 取消未支付订单 | POST `/{id}/cancel` | - | 200，状态 `CANCELLED` |
| O-10 | 取消已支付订单退款 | POST `/{id}/cancel`（部分/待撮合） | - | 200，状态 `CANCELLED`，冻结金额解冻退回 |
| O-11 | 订单列表 | GET `` | 可选 `?status=` | 200，`items/total/page/page_size` |
| O-12 | 订单详情 | GET `/{id}` | - | 200，含 `status_logs/matches/proof_urls/proof_submitted_at` |
| O-13 | 他人订单详情 | GET `/{id}`（非本人） | - | 404 `ORDER_NOT_FOUND` |
| O-14 | 撮合进度 | GET `/{id}/match` | - | 200，含 `remaining_amount` 与撮合明细 |

### 凭证上传（新增功能）

| 编号 | 用例 | 方法/路径 | 请求体/文件 | 预期结果 |
| --- | --- | --- | --- | --- |
| O-15 | 上传文件成功 | POST `/api/v1/upload` | `multipart/form-data` 文件字段名 `file`（.png） | 200，`data.url` 形如 `/uploads/1/xxxx.png` |
| O-16 | 未登录上传 | POST `/api/v1/upload` | 无 token | 401 |
| O-17 | 非法扩展名 | POST `/api/v1/upload` | 上传 `.exe` | 400 `FILE_TYPE_NOT_ALLOWED` |
| O-18 | 超限文件 | POST `/api/v1/upload` | 大于 10MB 文件 | 400 `FILE_TOO_LARGE` |
| O-19 | 静态访问文件 | GET `/uploads/1/xxxx.png` | 用 O-15 的 url | 200，返回图片内容 |
| O-20 | 绑定凭证 | POST `/orders/{id}/proof` | `{"urls":["/uploads/1/xxxx.png"]}` | 200，`proof_urls` 含该 url，`proof_submitted_at` 有值 |
| O-21 | 外链拒绝 | POST `/orders/{id}/proof` | `{"urls":["https://evil.com/x.png"]}` | 400 `INVALID_PROOF_URL` |
| O-22 | 非本人绑定 | POST `/orders/{id}/proof` | 他人 token | 404 `ORDER_NOT_FOUND` |
| O-23 | 卖单不可绑定 | POST `/orders/{卖单id}/proof` | 本人 token | 400 `ORDER_INVALID_STATUS` |
| O-24 | 未撮合不可绑定 | POST `/orders/{未支付订单}/proof` | 本人 token | 400 `ORDER_INVALID_STATUS` |

> 前提：O-20~O-24 需订单已撮合完成（`FULL_MATCHED`/`PROCESSING`/`COMPLETED`）。未支付的订单状态为 `WAITING_PAYMENT` 不可绑定。

---

## 5. 撮合模块 `/api/v1/matching`、`/orders` 撮合相关

| 编号 | 用例 | 方法/路径 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| M-01 | 全额撮合 | 用户 A 下买 10000 + 三家卖方各 4000 | A 支付触发撮合 | 订单 `COMPLETED`，`matched_amount=10000.00`，3 条撮合记录；买方余额扣 `10500`，卖方各收 4000 |
| M-02 | 部分撮合 | 卖方仅 4000，买方 10000 | 买方支付 | 订单 `PARTIAL_MATCHED`，`remaining_amount=6000.00` |
| M-03 | 人工撮合 | POST `/admin/orders/{id}/manual-match` | 超管执行 | 200，`matched` 布尔；有可撮合卖方则撮合 |
| M-04 | 服务期完成扫描 | 存在 PROCESSING 且 `created_at+service_days` 已过期的订单 | 等待调度（dev 60s）或手动触发 | 订单转 `COMPLETED`，`completed_order_count` 增加 |
| M-05 | 未到期不完成 | PROCESSING 且服务期未到 | 调度扫描 | 订单保持 `PROCESSING` |
| M-06 | 并发不超额 | 20 worker 同时撮合同一订单 | 后台并发调用 | 撮合总额恰等于目标额，无重复配对，买方冻结清零，卖方合计=目标额 |

---

## 6. 账户模块 `/api/v1/accounts`

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| ACC-01 | 账户查询 | GET `` | - | 200，`available_amount/frozen_amount/pending_amount` |
| ACC-02 | 交易流水 | GET `/transactions` | 可选 `?page=&page_size=` | 200，`items` 含 `business_type`（ADJUSTMENT/ORDER_PAYMENT/ORDER_SETTLEMENT/SERVICE_FEE/WITHDRAWAL/REFUND） |
| ACC-03 | 未登录访问 | GET `` | 无 token | 401 |

---

## 7. 财务模块 `/api/v1`（finance）

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| F-01 | 未实名申请提现 | POST `/withdrawals` | `{"amount":100,"usdt_address":"TX..."}` | 400 `REALNAME_REQUIRED` |
| F-02 | 低于最小金额 | POST `/withdrawals` | `{"amount":10,...}` | 400 `WITHDRAWAL_INVALID`（需先实名） |
| F-03 | 正常提现申请 | POST `/withdrawals` | 已实名 + 余额充足 | 200，`withdrawal_no`，金额冻结 |
| F-04 | 提现列表 | GET `/withdrawals` | - | 200，`items` 含 `PENDING` 状态 |
| F-05 | 费用配置 | GET `/fees` | - | 200，服务费率/最低/最高 |
| F-06 | 推广奖励记录 | GET `/promotion/records` | - | 200，推广奖励流水（若有） |

---

## 8. 工单 / 申诉 / 通知 / 公告 `/api/v1`（support）

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| S-01 | 创建工单 | POST `/support/tickets` | `{"category":"QUESTION","title":"问题","content":"详情","priority":"NORMAL"}` | 200，返回工单 |
| S-02 | 工单列表 | GET `/support/tickets` | - | 200，仅本人工单 |
| S-03 | 工单详情 | GET `/support/tickets/{id}` | - | 200，含 `messages` |
| S-04 | 回复工单 | POST `/support/tickets/{id}/messages` | `{"content":"回复内容"}` | 200，追加一条消息 |
| S-05 | 创建申诉 | POST `/appeals` | `{"order_id":x,"reason":"...","evidence":"..."}` | 200，返回申诉单 |
| S-06 | 申诉列表 | GET `/appeals` | - | 200 |
| S-07 | 通知列表 | GET `/notifications` | - | 200，`items` 含订单/支付通知 |
| S-08 | 已读单条 | POST `/notifications/{id}/read` | - | 200，该条 `read=true` |
| S-09 | 全部已读 | POST `/notifications/read-all` | - | 200 |
| S-10 | 公告列表 | GET `/announcements` | - | 200 |

---

## 9. 管理后台 `/api/v1/admin`（均需管理员 token）

### 9.1 登录与权限

| 编号 | 用例 | 方法/路径 | 预期结果 |
| --- | --- | --- | --- |
| AD-01 | 登录 | POST `/login` | 200，`data.admin.is_super=true`，token 含 `role=admin` |
| AD-02 | 我的信息 | GET `/me` | 200，`role_code=SUPER_ADMIN`，`permissions` 含全部权限 |
| AD-03 | **普通用户 token 访问后台（越权回归）** | GET `/me` | **401 `TOKEN_INVALID`**（修复后必须拒绝） |
| AD-04 | 匿名访问 | GET `/dashboard` 等 | 401 |
| AD-05 | 登录限流 | POST `/login` 连续 6 次 | 第 6 次起 429 `RATE_LIMITED` |

### 9.2 业务管理

| 编号 | 用例 | 方法/路径 | 请求体 | 预期结果 |
| --- | --- | --- | --- | --- |
| AD-10 | 数据看板 | GET `/dashboard` | - | 200，`user_total/order_total` 与图表 |
| AD-11 | 用户列表 | GET `/users` | `?page=&page_size=` | 200，`items` 含用户（手机号脱敏） |
| AD-12 | 用户详情 | GET `/users/{id}` | - | 200，`realname_name` 脱敏 |
| AD-13 | 冻结用户 | POST `/users/{id}/freeze` | `{"reason":"风控冻结"}` | 200；该用户 `GET /auth/me` 变 403 |
| AD-14 | 解冻用户 | POST `/users/{id}/unfreeze` | `{"reason":"复核通过"}` | 200；该用户恢复访问 |
| AD-15 | 调账入金/扣款 | POST `/users/{id}/adjust` | `{"amount":5000,"reason":"入金"}` | 200，用户可用余额变动，流水记 `ADJUSTMENT` |
| AD-16 | 实名审核 | POST `/realname/{ver_id}/review` | `{"approve":true,"reason":"资料齐全"}` | 200，状态通过，可提现 |
| AD-17 | 订单列表 | GET `/orders` | - | 200 |
| AD-18 | 订单详情 | GET `/orders/{id}` | - | 200，含 `proof_urls`（凭证可见） |
| AD-19 | 撮合队列 | GET `/matching` | - | 200，待撮合订单 |
| AD-20 | 撮合任务记录 | GET `/matching/jobs` | - | 200，`items` 含任务状态/处理数 |
| AD-21 | 账户查询 | GET `/accounts/{user_id}` | - | 200，目标用户余额 |
| AD-22 | 全部流水 | GET `/transactions` | - | 200 |
| AD-23 | 提现审核 | POST `/withdrawals/{id}/review` | `{"approve":true,"reason":"风控通过"}` | 200；拒绝则解冻退款 |
| AD-24 | 提现打款完成 | POST `/withdrawals/{id}/complete` | - | 200，冻结清零，流水记 `WITHDRAWAL` |
| AD-25 | 费用配置 | GET `/fees` / PUT `/fees/{fee_type}` | 修改费率 | 200 |
| AD-26 | 费率记录 | GET `/fees/records` | - | 200 |
| AD-27 | 推广配置 | GET `/promotions` | - | 200 |
| AD-28 | 风控事件 | GET `/risk/events` | - | 200 |
| AD-29 | 风控审核 | POST `/risk/events/{id}/review` | - | 200 |

### 9.3 客服/角色/系统

| 编号 | 用例 | 方法/路径 | 预期结果 |
| --- | --- | --- | --- |
| AD-30 | 工单列表 | GET `/tickets` | 200 |
| AD-31 | 工单回复 | POST `/tickets/{id}/reply` | 200 |
| AD-32 | 工单关闭 | POST `/tickets/{id}/close` | 200 |
| AD-33 | 申诉列表 | GET `/appeals` | 200 |
| AD-34 | 申诉处理 | POST `/appeals/{id}/process` | 200 |
| AD-35 | 管理员列表 | GET `/admins` | 200 |
| AD-36 | 新建管理员 | POST `/admins` | `{"username":"op1","password":"...","role_id":x}` | 200，新管理员登录仅见其角色权限 |
| AD-37 | 角色列表 | GET `/roles` | 200 |
| AD-38 | 新建角色 | POST `/roles` | `{"code":"TEST_ROLE","name":"测试","permission_codes":["order:view","user:view"]}` | 200 |
| AD-39 | 权限列表 | GET `/permissions` | 200，含 `order:view/user:view/...` |
| AD-40 | 操作日志 | GET `/logs` | 200 |
| AD-41 | 批量生成邀请码 | POST `/invites` | `{"count":3}` | 200，返回 3 个码 |
| AD-42 | 公告管理 | GET/POST `/announcements` | 发布公告 | 200 |

### 9.4 RBAC 校验（权限控制）

> 用 AD-36 建一个仅含 `order:view` 权限的普通管理员，验证权限隔离。

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| RB-01 | 该管理员访问 `/admin/orders`（有 `order:view`） | 200 |
| RB-02 | 该管理员访问 `/admin/users`（无 `user:view`） | 403 `PERMISSION_DENIED` |
| RB-03 | 该管理员操作 `/admin/orders/{id}/manual-match`（无 `order:match`） | 403 |

---

## 10. 信用等级 `/api/v1/credit`

| 编号 | 用例 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| C-01 | 初始等级 | 新用户 `GET /credit/level` | 200，`current.code=BASIC`，`next.code=BRONZE`，`need=3` |
| C-02 | 完成订单升级 | 完成 3 笔订单后查询 | 200，`current.code=BRONZE`，`progress` 为距下一级百分比 |
| C-03 | 进度计算 | 完成 5 笔（3→10 区间） | `progress≈29`，`need=5` |
| C-04 | 最高等级 | 完成 ≥80 笔 | `current.code=DIAMOND`，`next=null`，`progress=100` |

---

## 11. 安全专项回归（修复项）

| 编号 | 用例 | 预期结果 |
| --- | --- | --- |
| SEC-01 | 用户 token 访问 `/admin/me` | 401 `TOKEN_INVALID`（越权已修复） |
| SEC-02 | 生产环境默认密钥 | 设 `APP_ENV=production` 且 JWT_SECRET/SECRET_KEY 为占位值启动 | 启动即抛错拒绝 |
| SEC-03 | 系统邀请码注册 | 使用 `creator_id IS NULL` 的码注册 | 200，且不建立上级关系；MySQL 下无外键错误 |
| SEC-04 | 注册/登录限流 | 60 秒内超次数请求 | 429 `RATE_LIMITED` |
| SEC-05 | 密码存储 | 查库 `users.password_hash` | 为 `$argon2id$...` 哈希，非明文 |
| SEC-06 | CORS | 非白名单 Origin 带凭据请求 | 被拒绝（dev 白名单为 localhost:5173/5174） |
| SEC-07 | 提现并发 | 同一笔提现并发审核/打款 | 仅成功一次，金额不重复扣减 |
| SEC-08 | 邀请码 FK | 删除用户后其码 `creator_id` 可空 | 无外键引用错误 |

---

## 12. 端到端主流程（冒烟）

| 步骤 | 操作 | 预期 |
| --- | --- | --- |
| 1 | 系统码注册新账号 → 登录 | 成功，返回 token |
| 2 | 管理员给新账号入金 5000 | 可用余额 5000.00 |
| 3 | 新账号下买单 1000（金额自定义） | `WAITING_PAYMENT`，应付 1050 |
| 4 | 支付 | 进入撮合；有卖方则撮合完成 |
| 5 | 撮合完成后上传凭证图片 | 返回 `/uploads/...` url，绑定成功 |
| 6 | 后台查看该订单 | 详情含凭证图片，可预览 |
| 7 | 新账号查信用等级 | 完成 1 笔，进度 33%（距铜牌还需 2 笔） |

---

## 13. 自动化测试对照

| 测试文件 | 覆盖用例 | 数量 |
| --- | --- | --- |
| `tests/integration/test_auth_flow.py` | 注册/登录/刷新/切换 | 9 |
| `tests/integration/test_account.py` | 入金/流水/余额不足 | 2 |
| `tests/integration/test_order_matching.py` | 全额/部分/取消/重复支付 | 4 |
| `tests/integration/test_withdrawal.py` | 实名/提现全流程/最小金额 | 3 |
| `tests/api/test_admin_api.py` | 后台登录/看板/RBAC/越权回归 | 5 |
| `tests/api/test_upload.py` | 上传/凭证绑定/边界 | 6 |
| `tests/unit/test_security.py` | 哈希/JWT/脱敏 | 5 |
| `tests/unit/test_fee.py` | 服务费计算 | 6 |
| `tests/unit/test_credit.py` | 信用等级 | 4 |
| `tests/unit/test_invite.py` | 邀请码 | 2 |
| `tests/matching/test_concurrency.py` | 并发撮合 | 1 |

运行：`cd backend && $env:PYTHONPATH="C:\Users\l1910\Desktop\TX\backend" ; python -m pytest`（当前 **49 用例全部通过**）。

---

*文档结束。测试前请先执行后端迁移：`python -m alembic upgrade head`，并确认 `GET /health` 返回 200。*